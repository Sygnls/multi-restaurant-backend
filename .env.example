# App
PORT=4000
ALLOWED_ORIGIN=http://localhost:3000

# JWT (reserved for later steps)
JWT_SECRET=change-me

# DB
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=mt_ordering

# For docker-compose local dev
MYSQL_ROOT_PASSWORD=
